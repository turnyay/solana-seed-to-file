# solana seed to file

Quick way to convert a mnemonic seed + derivation path to keypair json file

```
cp .env.example .env 
(edit env with seed + path)
npm install
npm run saveFile
```

File saved:
keypair.json
